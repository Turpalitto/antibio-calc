# P5.6 Owner Recovery Closure Program — Post Personal-Path-Fix Report

Generated: 2026-07-15. **Still STOP — no files have been staged or committed.** This supersedes the prior "READY FOR STAGING APPROVAL" report after patching the two confirmed portability defects the owner flagged.

## What was fixed

`src/pipeline/extraction/layout.py:41` and `src/pipeline/extraction/semantic.py:17` hard-coded `user_site = r'C:\Users\TURPAL\AppData\Roaming\Python\Python312\site-packages'` and unconditionally inserted it into `sys.path`. Replaced with:

```python
_external_site_packages = os.environ.get("ANTIBIO_EXTERNAL_SITE_PACKAGES", "").strip()
if _external_site_packages:
    if not os.path.isdir(_external_site_packages):
        raise RuntimeError(
            "ANTIBIO_EXTERNAL_SITE_PACKAGES is set but does not point to an "
            f"existing directory: {_external_site_packages!r}"
        )
    if _external_site_packages not in sys.path:
        sys.path.insert(0, _external_site_packages)
```

- **A. Normal imports from the active environment** — default behavior, no path injection.
- **B. Explicit `ANTIBIO_EXTERNAL_SITE_PACKAGES` opt-in** — only added when the owner sets it in their local, uncommitted `.env`; documented with an empty placeholder in `.env.example`.
- **C. Clear actionable failure** — an invalid path raises `RuntimeError` naming both the variable and the bad path; nothing silently no-ops.
- **No redesign**: only the 4-line `user_site` block in each file was touched (plus adding `import os` to `semantic.py`, which didn't have it). No try/except import-fallback logic, model-loading code, or clinical logic was touched. No databases, PDFs, models, or review data were touched.

## Why this preserves extraction behavior

The full targeted extraction test suite — including the real-model, non-skipped path (`test_layout_produces_structured_tables_real`) — was run **after** the fix and passed unchanged (13/13). Investigation of the test output showed `torch` and other heavyweight packages are actually installed in the owner's per-user site-packages directory, which **Python's own `site` module already adds to `sys.path` automatically** for per-user installs — meaning the old hard-coded insert was redundant even on the machine it was written for. Removing it has zero observed effect on extraction behavior.

## Requirement 6 — full sweep of the proposed tracked set

| Match | Classification |
|---|---|
| `layout.py`, `semantic.py` | **Fixed**, no longer match |
| `AI_LOG.md:759` | Generated historical evidence (log of a command actually run) |
| `PRODUCTION_READINESS_AUDIT_2026-07-15.md:292` | Generated historical evidence (documents the now-fixed defect as it existed at audit time) |
| `docs/extraction/README.md:73-83` | Generated historical evidence (verification-session transcript) |
| `build_p44_kb.py`, `main.py`, `production_reprocessor.py`, `reprocess_p45_layout.py`, `test_knowledge_base.py`, `test_layout_table_provenance.py` (`sys.path.insert` with `__file__`-relative or `"."` paths) | False positive — relative/dynamic, not hard-coded absolute literals |
| `.env.example` (`ANTIBIO_CORPUS_DIR=C:\clinrec_downloader`) | Legitimate documentation example — placeholder, no username |

**Zero hard-coded personal paths remain in production source. Zero hidden workstation dependency remains** (per the site-module finding above, the dependency was never load-bearing to begin with).

## Regression tests added

`src/tests/test_extraction_site_packages.py` (9 tests): production source contains no personal path; normal import succeeds absent the override; an invalid override fails with a `RuntimeError` naming the variable; a valid override is accepted; an empty-string override behaves identically to absent. All 9 pass.

## Test results

| Suite | Result |
|---|---|
| `src/tests/test_extraction_site_packages.py` (new) | **9 passed** |
| `test_layout_table_provenance.py` + `test_document_extraction.py` (targeted, real-model path included) | **13 passed** |
| Import smoke test (`layout`, `semantic`, `router`) | **clean** |
| `clinical_engine/regimen/tests` + `clinical_engine/review_workbench/tests` | **93 passed** |
| Canonical `pytest --collect-only` | **1396 tests collected** (was 1387; +9 from the new regression file), zero collection errors |
| Secret scan (tracked set + full Git history) | **clean** |
| Personal-path scan (tracked set) | **clean** — see requirement-6 table above |
| Large-file scan | **clean** — no file >1 MB |
| `.gitignore` validation | **clean** — both prior gaps fixed and re-proven |

No unrelated source, config, or test file was modified. Changed/added files this round: `src/pipeline/extraction/layout.py`, `src/pipeline/extraction/semantic.py`, `.env.example`, `src/tests/test_extraction_site_packages.py` (new), `REPRODUCIBLE_BUILD.md`, `REPOSITORY_MANIFEST.md`, `REPOSITORY_MANIFEST.json`, `DEPENDENCY_LOCK_VERIFICATION.md`, `REPOSITORY_RECOVERY_CLASSIFICATION.csv`/`.md`, `P56_PROPOSED_TRACKED_FILES.txt`, `P56_PROPOSED_IGNORED_FILES.txt`, `P56_PROPOSED_OWNER_DECISIONS.md`, this report.

## Final proposed tracked set

- **453 files, 6,956,194 bytes (6.96 MB)** — see [P56_PROPOSED_TRACKED_FILES.txt](P56_PROPOSED_TRACKED_FILES.txt)
- No file exceeds 1 MB
- **66 files excluded**, all individually decided, none deleted from disk: 60 pilot-review packet JSONs (`pilot_review_batch*/crt_*.json`), `unknown_drugs.csv` ×2, `SESSION_HISTORY.md`, `SESSION_SUMMARY.md`, `CORPUS_MANIFEST.json`, `REPOSITORY_UNTRACKED_INVENTORY.json`, `RECOVERY_TRACKED_FILES.txt` — see [P56_PROPOSED_IGNORED_FILES.txt](P56_PROPOSED_IGNORED_FILES.txt)
- Zero files remain in classification category R, Q, or T
- Zero secrets, zero runtime databases, zero PDFs, zero model/cache files, zero `.env` files, zero legacy `unknown_drugs.csv`, zero generated pilot packet JSONs in the proposed tracked set

---

## Required return value

**`READY FOR STAGING APPROVAL`**

All conditions for this status are met:
- Zero hard-coded personal paths remain in production source.
- No hidden workstation dependency remains.
- Tests pass (9 + 13 + 93 = 115 directly relevant, plus clean 1396-test canonical collection).
- Proposed tracked set (453 files, 6.96 MB) remains free of secrets, databases, PDFs, models, caches, `.env` files, legacy `unknown_drugs.csv` files, and generated pilot packet JSONs.

Waiting for the exact phrase **`APPROVE P5.6 REPOSITORY RECOVERY STAGING`** before any `git add` is executed.
