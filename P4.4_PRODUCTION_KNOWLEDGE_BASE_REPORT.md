# P4.4 PRODUCTION KNOWLEDGE BASE PLATFORM — ENTERPRISE RELEASE REPORT (REGENERATED)
**Date:** 2026-07-15
**Mode:** STRICT — real execution, measured data only, code freeze respected during audit
**Supersedes:** the 2026-07-13 version of this report (stale: claimed 116/204 objects from a
3-PDF checkpoint accumulation — see `docs/governance/CURRENT_PROJECT_STATE.md` AUDIT ADDENDUM).
**Status:** Clean, reproducible, full-corpus rebuild complete and certified. See
`PROVENANCE_CERTIFICATION.md` and `PRODUCTION_SCORECARD.md` for the full evidence trail.

## Executive Summary
The Production Knowledge Base (P4.4) was rebuilt from a clean slate on the fully-audited pipeline
(PR-001 table-loss fix + provenance-contract v2 migration + RC-016/017/018 fixes, each independently
adversarial-audited — see `ROOT_CAUSE_REGISTER.md`). This is the **first reproducible, complete**
build of the full 192-PDF corpus with a fully-verified provenance contract.

## Real run data (192/192 PDFs, deterministic checkpoint-driven build)
- **31,336 Knowledge Objects**: Dose 24,705 · Medication 2,788 · Evidence 2,190 · Contraindication
  841 · Recommendation 381 · Diagnosis 431.
- **28,368 active, 2,968 superseded** (version conflicts correctly detected + superseded, not lost).
- **100,854 provenance rows** across 192 distinct source PDFs, all mapped to a `guideline_id`.
- **6,574 open reviews** (conflict/low-validation queue — expected at this scale, not a defect).
- **466 table-derived provenance rows** (`layout_engine != 'none'`): 451 via
  `table-transformer+rapidtable`, 15 via the RapidTable fallback (RC-016 fix confirmed live).
- Build time: multi-hour, single-process, CPU-bound layout inference (per-PDF range ~1s to ~1500s
  for the heaviest table-dense guideline); see `p44_kb_rebuild_FINAL_20260714.log`.

## Provenance contract — measured, full scale
| Field | NULL count (of 100,854) |
|---|---|
| original_text | 0 |
| guideline_id | 0 |
| extractor | 0 |
| layout_engine | 0 |
| semantic_engine | 0 |
| schema_version | 0 (100% = 2) |

Distinct `guideline_id`: 192 (100% of the corpus mapped). INV-17 (table_row ⇒ table_conf): 0
violations. Full detail: `PROVENANCE_CERTIFICATION.md`.

## Architectural invariants — full KB
All blocking invariants PASS (INV-01/02/03/08/09/10/12 = 0 violations). INV-05 (dose unit) is
advisory-WARN (8,412 rows, tracked as RC-008 normalization work, not blocking). Full report:
`inv_report_FINAL.json`.

## Clinical Knowledge Yield (CKY) — baseline established
`CKY.overall = 10.3%` (332/3,220 measured candidates, 3-PDF representative sample). Dominant loss
reason: **Duplicate (86.5%)** — most candidates already exist in the KB from prior mentions in the
same/other guidelines (expected at this corpus density). `CKY.tables = 1.1%` confirms RC-009
(table-derived AlternativeTherapy/FirstLineTherapy/AgeRestriction dropped by the KB builder) as
measured fact, not conjecture. Full histogram: `pr008_yield_FINAL.json`.

## Knowledge Coverage (delivered) — full corpus
dose/route/contraindications/evidence = 100% (192/192 PDFs); drug = 97.9%; alternatives 58.3%;
diagnosis 56.8%; first_line 28.6%; renal 24.0%; pregnancy 23.4%; **duration/frequency/pediatric =
0%** (real, measured gap — not yet extracted by the semantic layer). Full report:
`coverage_FINAL.json`.

## Regression
72 passed, 1 xfailed (expected), 0 failed. `regression_FINAL.log`.

## Scope boundary — IMPORTANT
This report certifies the **Production Knowledge Base** (`kb_p44.db`) — its build reproducibility,
provenance integrity, and internal consistency. It does **not** certify that the Clinical Decision
Engine (`clinical_engine/`) serves recommendations sourced from this KB: as of this report, the
engine reads a separate `medical_normalizer.db`/`normalized_regimens.sqlite`, unconnected to
`kb_p44.db` (see `ENTERPRISE_ARCHITECTURE_REVIEW.md` EAR-1, registered as **RC-019, Critical**).
Closing this integration gap is scoped to P5/P6, not P4.4.

## References
`PRODUCTION_SCORECARD.md` (category verdicts) · `PROVENANCE_CERTIFICATION.md` (contract
certification) · `ROOT_CAUSE_REGISTER.md` (all findings) · `ENTERPRISE_ARCHITECTURE_REVIEW.md`
(whole-system risks) · `p44_kb_rebuild_FINAL_20260714.json` (raw build stats).
