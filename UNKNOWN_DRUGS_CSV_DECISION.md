# unknown_drugs.csv Decision — Phase 4

Generated: 2026-07-15.

## Finding: two distinct files share this name

| Path | Tracking state | Schema | Rows |
|---|---|---|---|
| `./unknown_drugs.csv` | **gitignored** (`.gitignore:83`, `/unknown_drugs.csv`) | `original_name, occurrences, possible_normalization, atc_candidate` | 441 |
| `medical_dictionary/unknown_drugs.csv` | **untracked, not ignored** — currently addable | `original_name, occurrences, affected_guidelines, affected_diagnoses, possible_normalization, possible_atc` | 441 |

The `medical_dictionary/` copy has two extra columns (`affected_guidelines`, `affected_diagnoses`) and is a superset/later revision of the root copy. They are not identical files coincidentally sharing a name — they represent the same underlying audit at two different stages.

## Answers to the required questions

- **Who produces it?** No producer script exists anywhere in the current tracked or untracked source tree (`src/`, `clinical_engine/`, `medical_dictionary/`, `medical_normalizer/`). A targeted grep for CSV-writing code referencing this schema (`csv.writer`/`to_csv` combined with `original_name`/`possible_normalization`) returned nothing. It was generated by a process that is not currently checked in.
- **Does application code consume it?** No. Grep for the literal filename `unknown_drugs.csv` across all `.py`/`.ps1`/`.js` files returns zero matches.
- **Do tests consume it?** No — same result, no test references it.
- **Is it deterministic?** Unknown — without the producer script, it cannot be regenerated or verified against current `db/` content.
- **Does it contain clinical decisions?** No approvals — it contains *candidate* normalizations and ATC-code guesses for drug names the pipeline couldn't auto-match, several explicitly flagged `[требует ручной проверки]` ("requires manual review") in the source text. This is raw candidate data, not an approved clinical decision.
- **Is it generated from source databases?** Presumably yes (it's phrased as counts of unmatched drug-name occurrences), but this can't be confirmed without the producer.
- **Does it contain sensitive data?** No — drug names and counts only, no patient or credential data.
- **Downstream consumer that *is* real:** [dictionary_expansion.md](dictionary_expansion.md:4) documents that `unknown_drugs.csv` (441 names) was the **input** to a manual dictionary-expansion review, whose **output** is `medical_dictionary/dictionary_candidates.json` — which **is** consumed by code ([medical_normalizer/dictionary.py](medical_normalizer/dictionary.py), [medical_dictionary/__init__.py](medical_dictionary/__init__.py), [clinical_engine/tools/build_decision_ledger.py](clinical_engine/tools/build_decision_ledger.py)). In other words, `unknown_drugs.csv` is upstream scratch data for a process that has already produced its real, tracked-worthy output elsewhere.

## Recommendation

**E. OWNER DECISION REQUIRED**

Neither A (track as authoritative source — no, it's not consumed and its producer is missing), nor C (generate during build — no build script exists to do so), nor D (exclude as pure runtime artifact — plausible but the file *did* drive a real, already-completed dictionary-expansion review, so discarding it loses the audit trail for how `dictionary_candidates.json` was derived) can be selected with confidence from repository evidence alone.

Two sub-decisions the owner needs to make explicitly:
1. **Root `./unknown_drugs.csv`** (gitignored today) — leave gitignored (recommended: it's clearly a scratch/generated output per its existing `.gitignore` entry), or explicitly un-ignore if the owner wants it as a permanent audit-trail fixture.
2. **`medical_dictionary/unknown_drugs.csv`** (currently untracked and stage-eligible) — this is the more complete revision. Options: (B) track as a fixed historical fixture documenting the dictionary-expansion audit trail, or (D) delete it as a superseded scratch artifact now that `dictionary_candidates.json` exists.

**Until the owner decides, `medical_dictionary/unknown_drugs.csv` is excluded from the Phase 8 proposed tracked-file set.**

## Owner decision received (2026-07-15)

**C. GENERATE DURING BUILD.** Neither file is tracked as an authoritative clinical source. Both remain classified as **legacy/runtime artifacts**, excluded from the recovery commit, and are **not deleted from disk**.

Verification performed before staging, per the owner's explicit conditions:

- **No production or test code requires either file.** Confirmed by exact-filename grep (`unknown_drugs.csv`) across every `.py`, `.ps1`, and `.js` file in the repository, tracked and untracked — zero matches, for both the root-level and `medical_dictionary/` copies.
- **Expected producer, schema, output path:** no producer script exists anywhere in the current source tree. This is **not fabricated**. What can be documented from evidence alone:
  - Root `./unknown_drugs.csv` schema: `original_name, occurrences, possible_normalization, atc_candidate` (441 rows) — already gitignored (`/unknown_drugs.csv`).
  - `medical_dictionary/unknown_drugs.csv` schema: `original_name, occurrences, affected_guidelines, affected_diagnoses, possible_normalization, possible_atc` (441 rows) — a later revision with two additional columns.
  - Both are documented in [dictionary_expansion.md](dictionary_expansion.md) as the **input** to a one-time manual dictionary-expansion review whose completed **output**, `medical_dictionary/dictionary_candidates.json`, is the file actually consumed by application code (`medical_normalizer/dictionary.py`, `medical_dictionary/__init__.py`, `clinical_engine/tools/build_decision_ledger.py`).
  - No deterministic regeneration command can be stated, because the producer is missing.
- **Missing deterministic producer registered as a non-blocking follow-up**, since the application does not depend on either file (`dictionary_candidates.json` already exists and is the real, tracked artifact). This follow-up is recorded here rather than as a fabricated build step: *if* the drug-name-normalization audit needs to be re-run in the future (e.g., against an updated `db/` corpus), a script reproducing the `original_name, occurrences, possible_normalization, atc_candidate` counts from `db/diseases/*.json` content would need to be written from scratch — it does not exist today.
- **Classification:** both files are marked **legacy/runtime artifact** in [REPOSITORY_RECOVERY_CLASSIFICATION.csv](REPOSITORY_RECOVERY_CLASSIFICATION.csv), `safe_to_commit = no`.
- **Staging:** both files are excluded from [P56_PROPOSED_TRACKED_FILES.txt](P56_PROPOSED_TRACKED_FILES.txt).
